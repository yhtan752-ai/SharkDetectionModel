
tracker_type: bytetrack 
track_high_thresh: 0.5    # threshold for the first association
track_low_thresh: 0.1     # threshold for the second association
new_track_thresh: 0.6     # threshold for init new track if the detection does not match any existing track
track_buffer: 100         # THE FIX: Increase this! Number of frames to keep a lost track in memory
match_thresh: 0.8  
fuse_score: True       # threshold for matching tracks